language: node_js

node_js:
  - 'node'
  - '7'
  - '6'

jobs:
  include:
    script: npm build
    script: npm test
    deploy:
      provider: npm
      email: $NPM_EMAIL
      api_key: $NPM_API_TOKEN
      skip_cleanup: true
      on:
        tags: true
